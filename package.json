{
  "name": "@hyphen-network/devassets",
  "version": "1.13.260619",
  "description": "Local-first developer secrets manager — store encrypted env vars, manage API keys across projects, resolve provider identities, and inject secrets into commands. CLI + MCP server. No cloud, no account.",
  "type": "module",
  "bin": {
    "devassets": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "files": [
    "dist/",
    "skills/",
    "README.md",
    "CHANGELOG.md"
  ],
  "scripts": {
    "build": "tsc && chmod +x dist/index.js",
    "build:ui": "cd ui && npm run build",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "prepublishOnly": "npm run build && npm test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "chalk": "^5.3.0",
    "commander": "^12.1.0",
    "js-yaml": "^4.1.0",
    "listr2": "^10.2.1",
    "ora": "^9.4.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.15.0",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "keywords": [
    "secrets-manager",
    "env-manager",
    "dotenv",
    "api-key-manager",
    "developer-secrets",
    "local-secrets",
    "environment-variables",
    "vault",
    "cli",
    "mcp",
    "devtools",
    "credential-management",
    "env",
    "secrets",
    "developer-tools",
    "multi-project",
    "indie-dev",
    "vercel",
    "supabase",
    "claude-code"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/0xRyanlee/devassets"
  }
}
