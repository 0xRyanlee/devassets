node_modules/
dist/
ui/dist/
ui/node_modules/
*.js.map
.env
.env.local
.DS_Store
*.db
*.sqlite
.claude/
# CI workflow deferred: requires GitHub token with workflow scope to push
# Run: gh auth refresh -h github.com -s workflow && git add .github/ && git commit && git push
.github/
